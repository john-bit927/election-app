<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>PU Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .election-card { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border-radius: 15px; box-shadow: 0 8px 32px rgba(0,0,0,0.1); }
    .result-item { background: white; margin: 10px 0; border-radius: 10px; padding: 15px; box-shadow: 0 4px 8px rgba(0,0,0,0.05); transition: transform 0.2s; }
    .result-item:hover { transform: translateY(-2px); }
    .party-color { padding: 5px 10px; border-radius: 20px; font-weight: bold; }
    .pdp { background: #e91e63; color: white; }
    .dpp { background: #9c27b0; color: white; }
    .acn { background: #f44336; color: white; }
    .ppa { background: #ff9800; color: white; }
    .cdc { background: #4caf50; color: white; }
    .jp { background: #2196f3; color: white; }
    .cpc { background: #ffeb3b; color: black; }
    .anpp { background: #795548; color: white; }
    .accord { background: #607d8b; color: white; }
  </style>
</head>
<body class="container mt-4">
  <div class="election-card p-4 mb-4 text-center">
    <h2><i class="fas fa-vote-yea me-2"></i>Individual Polling Unit Results</h2>
  </div>
  <form method="post" action="/pu" class="mb-3">
    <div class="input-group w-50 mx-auto">
      <span class="input-group-text"><i class="fas fa-search"></i></span>
      <input type="number" name="pu_id" placeholder="Enter PU ID (e.g., 8)" value="<%= puId || '' %>" class="form-control" required>
      <button type="submit" class="btn btn-primary"><i class="fas fa-download me-1"></i>Load Results</button>
    </div>
  </form>
  <% if (pu) { %>
    <div class="card mb-4">
      <div class="card-header bg-primary text-white text-center">
        <h4><i class="fas fa-map-marker-alt me-2"></i>Results for: <%= pu.polling_unit_name %> (ID: <%= pu.uniqueid %>)</h4>
      </div>
      <div class="card-body">
        <div class="row">
          <% Object.entries(results).forEach(([party, score]) => { %>
            <div class="col-md-6">
              <div class="result-item d-flex justify-content-between align-items-center">
                <span class="party-color <%= party.toLowerCase() %>"><%= party %></span>
                <span class="fs-5 fw-bold"><%= score %></span>
              </div>
            </div>
          <% }); %>
        </div>
      </div>
    </div>
  <% } else if (puId) { %>
    <div class="alert alert-warning text-center"><i class="fas fa-exclamation-triangle me-2"></i>No data for PU ID <%= puId %>. Try 8.</div>
  <% } %>
  <div class="text-center">
    <a href="/" class="btn btn-secondary"><i class="fas fa-home me-1"></i>Home</a>
  </div>
</body>
</html>